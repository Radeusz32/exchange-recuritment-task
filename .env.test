# define your env variables for the test env here
KERNEL_CLASS='App\Kernel'
APP_SECRET='$ecretf0rt3st'

# Integration tests run against their own database (the _test suffix comes from
# config/packages/doctrine.yaml), which the application user has no rights to create.
DATABASE_URL="mysql://root:root@mariadb:3306/app?serverVersion=11.4.0-MariaDB&charset=utf8mb4"
